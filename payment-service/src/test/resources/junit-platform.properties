# Auto-register test extensions declared via META-INF/services (PaymentDatabaseResetExtension),
# so every IT starts from a clean, isolated database in the shared Testcontainers Postgres.
junit.jupiter.extensions.autodetection.enabled=true
