# Stripe test-mode credentials — copy to `.env` and fill in (never commit `.env`).
# Use sk_test_… / pk_test_… only. Rotate keys if they were ever exposed.

STRIPE_API_KEY=sk_test_replace_me
STRIPE_PUBLISHABLE_KEY=pk_test_replace_me
STRIPE_WEBHOOK_SECRET=whsec_replace_me
STRIPE_MODE=real
