FROM node:20-alpine AS build
WORKDIR /app
# Copy the lockfile and use `npm ci` so the image is built from exactly the committed
# dependency set (matches risk-model's `uv sync --frozen`); `npm install` would silently
# resolve newer versions and drift from the lockfile.
COPY package.json package-lock.json ./
RUN npm ci
COPY . .
ARG VITE_API_BASE_URL=http://localhost:8080
ENV VITE_API_BASE_URL=$VITE_API_BASE_URL
RUN npm run build

# nginx-unprivileged, not stock nginx: the stock image assumes it starts as root (it needs to
# create /var/cache/nginx/* and the pid file), so forcing `USER nginx` on it makes the container
# crash-loop with "mkdir /var/cache/nginx/client_temp failed (13: Permission denied)". This variant
# is built to run as uid 101 with those paths already writable, and defaults to listening on 8080.
FROM nginxinc/nginx-unprivileged:1.27-alpine
COPY nginx.conf /etc/nginx/conf.d/default.conf
COPY --from=build /app/dist /usr/share/nginx/html
EXPOSE 8080
HEALTHCHECK --interval=10s --timeout=3s --start-period=5s --retries=5 \
  CMD wget -q -O /dev/null http://127.0.0.1:8080/ || exit 1
